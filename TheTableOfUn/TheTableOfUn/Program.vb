Imports System
Imports Terminal.Gui

Module Program
    Sub Main(args As String())
        Console.Title = "The Table of Un (aka: Un's Table)"
        Application.Init()
        SplashScreen.Run()
        MainMenu.Run()
    End Sub
End Module
